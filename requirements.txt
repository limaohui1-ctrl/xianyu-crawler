# 通用网站采集中心 — 运行依赖
# Python 3.11+ required (tested on 3.11.15)
#
# 安装运行依赖: pip install -r requirements.txt
# 安装开发依赖: pip install -r requirements.txt -r requirements-dev.txt
# 安装浏览器:    python -m playwright install chromium

# ── GUI framework ──
PyQt6>=6.7,<7

# ── web scraping ──
beautifulsoup4>=4.12
httpx>=0.27

# ── Excel export ──
openpyxl>=3.1

# ── browser automation ──
playwright>=1.45

# ── PDF support (optional, for file-to-table extraction) ──
PyPDF2>=3.0
pypdf>=4.0

# ── Windows notification (legacy xianyu mode, optional) ──
# win10toast>=0.9
